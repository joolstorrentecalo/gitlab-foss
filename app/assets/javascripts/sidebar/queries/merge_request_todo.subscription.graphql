subscription issuableTodoUpdated($userId: UserID!, $issuableId: IssuableID!) {
  issuableTodoUpdated(issuableId: $issuableId, userId: $userId) {
    ... on MergeRequest {
      id
      currentUserTodos(state: pending) {
        nodes {
          id
        }
      }
    }
  }
}
